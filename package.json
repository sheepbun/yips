{
  "name": "yips-root",
  "version": "1.0.0",
  "description": "Root package for Yips CLI",
  "scripts": {
    "start": "npm start --prefix app",
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app"
  }
}
